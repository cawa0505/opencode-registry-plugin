# code review profile — read-only analysis tools
# Activate: /registry switch review (in-session)
name: review
description: "Code review — read-only analysis without write/web tools"
tags:            # capability tags this profile steers toward
  - code
  - search
  - read
  - nav
servers:
  exclude:       # MCP server names to down-rank (soft-gate)
    - write
    - web
    - media
    - utility
