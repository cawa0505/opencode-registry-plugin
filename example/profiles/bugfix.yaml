# bugfix profile — focus on debugging and code analysis tools
# Activate: /registry switch bugfix (in-session)
name: bugfix
description: "Debugging session with code analysis, search, and web research tools"
tags:            # capability tags this profile steers toward
  - code
  - search
  - read
  - debug
  - web
servers:
  exclude:       # MCP server names to down-rank (soft-gate)
    - media
    - utility
